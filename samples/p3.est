/// Example 3: Test Case 'for' statement

/// Loop 1
for(i = 0 to 2 step 1)
{
  check(i, i+1);
  check(i-1, i+1);
  check(i+1, i-1);
  check(i+1, i+1);
}
update;

/// Loop 2: the same as loop 1
for(i = 0 to 2 step 1)
{
  check(i, i+1);
  check(i-1, i+1);
  check(i+1, i-1);
  check(i+1, i+1);
}
update;
 
/// Loop 3
for(i = -2 to 1 step 1)
{
  check(i, i+1);
  check(i-1, i+1);
  check(i+1, i-1);
  check(i+1, i+1);
}

update;
 